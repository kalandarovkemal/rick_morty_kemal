name: rick_morty_kemal
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  domain:
    path: ./domain
  core:
      path: ./core
  core_ui:
      path: ./core_ui
  data:
      path: ./data
  navigation:
      path: ./navigation


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.2

flutter:
uses-material-design: true































